# Installation Decision

Audited source will be installed as readable text files on the isolated `rebuild-v1-clean` branch. The chunked archive transport is deprecated after failing its integrity check. Legacy `main` remains untouched.