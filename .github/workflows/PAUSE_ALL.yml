name: PAUSE ALL WORKFLOWS
# NOTE: This workflow does NOT actually pause or control other workflows.
# It's a notice-only workflow to document when workflows are manually disabled.
# To resume after disabling workflows, see: WORKFLOW_OUTAGE_RECOVERY.md
on:
  workflow_dispatch:
  
jobs:
  pause-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Workflows Paused
        run: |
          echo "All workflows paused by Director"
          echo "Imperial Physics Observatory in maintenance mode"

