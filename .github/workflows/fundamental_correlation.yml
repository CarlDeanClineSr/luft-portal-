name: Run Fundamental Constants Correlation

on:
  schedule:
    - cron: '0 * * * *'  # Hourly
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install numpy scipy
      - name: Run script
        run: python scripts/fundamental_constants_correlation.py > results/fundamental_matches.txt
      - name: Commit results
        run: |
          git config user.name "LUFT Bot"
          git config user.email "bot@luft.local"
          git add results/fundamental_matches.txt
          git diff --quiet && git diff --staged --quiet || git commit -m "Update fundamental matches - $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
